# Models

Base/shared model interfaces (auth, API envelope) live here. Per-module domain
models (Lead, Client, Matter, Invoice, ...) are added as placeholder folders per
module in later Build Playbook prompts — this directory is the seed, not the
final shape.
