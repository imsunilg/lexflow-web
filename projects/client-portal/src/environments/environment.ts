// See projects/staff-portal/src/environments/environment.ts for the full
// rationale — same pattern, portal audience (`/api/portal/v1`, distinct JWT
// audience per PRD Module 17).
export const environment = {
  apiBaseUrl: '/api/portal/v1',
};
